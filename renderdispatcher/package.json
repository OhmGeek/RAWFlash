{
  "name": "renderdispatcher",
  "version": "1.0.0",
  "description": "An intermediary between the web editor client and the render servers. Allows for load balancing via RabbitMQ.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sleep 10 && node index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.5.2",
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  }
}
